server {
  listen 8080;
  root /usr/share/nginx/html;

  location /api/ {
    proxy_pass http://chat-flow-api:3001;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  location /health {
    proxy_pass http://chat-flow-api:3001/health;
  }

  location / {
    try_files $uri /index.html;
  }
}
